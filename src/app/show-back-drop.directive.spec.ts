import { ShowBackDropDirective } from './show-back-drop.directive';

describe('ShowBackDropDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowBackDropDirective();
    expect(directive).toBeTruthy();
  });
});
